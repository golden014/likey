{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "likey",
  "scripts": {
    "generate": "./did.sh && dfx generate",
    "gen-deploy": "./did.sh && dfx generate && dfx deploy -y",
    "dev": "next dev ./src/likey_frontend",
    "build": "next build ./src/likey_frontend",
    "start": "next start ./src/likey_frontend",
    "lint": "next lint ./src/likey_frontend"
  },
  "type": "module",
  "workspaces": [
    "src/likey_frontend"
  ]
}
